# CalendarBot Core Dependencies
# Generated from pyproject.toml for compatibility with legacy tools

# Core calendar and HTTP functionality
icalendar>=5.0.0
httpx>=0.25.0
aiosqlite>=0.19.0

# Data validation and settings
pydantic>=2.0.0
pydantic-settings>=2.0.0
PyYAML>=6.0

# Date and time handling
python-dateutil>=2.8.0
pytz>=2023.3

# Security and scheduling
cryptography>=41.0.0
APScheduler>=3.10.0
psutil>=5.8.0

# Cache dependencies for optimization
cachetools>=5.0.0
diskcache>=5.0.0

# Image processing moved to optional 'epaper' extras to avoid heavy native deps on constrained devices
# Install with: pip install -e .[epaper] or pip install .[epaper]

# HTTP client for kiosk health checks
aiohttp>=3.8.0

# Framebuffer UI dependencies
pygame>=2.5.0

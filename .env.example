# CalendarBot Environment Configuration
# Copy this to .env and customize with your real values

# ============================================================================
# CALENDARBOT_LITE CONFIGURATION (calendarbot_lite/ directory)
# ============================================================================
# These variables are used by the lightweight calendarbot_lite application

# ICS Calendar URL (Required for calendarbot_lite)
CALENDARBOT_ICS_URL=https://outlook.office365.com/owa/calendar/your-calendar-id/calendar.ics

# Server Configuration
CALENDARBOT_WEB_HOST=0.0.0.0
CALENDARBOT_WEB_PORT=8080

# Refresh interval in seconds (or use CALENDARBOT_REFRESH_INTERVAL_SECONDS)
CALENDARBOT_REFRESH_INTERVAL=300

# Alexa Integration (optional)
# CALENDARBOT_ALEXA_BEARER_TOKEN=your-alexa-bearer-token

# Rate Limiting Configuration (optional - defaults shown)
# Protects Alexa endpoints from DoS attacks
# CALENDARBOT_RATE_LIMIT_PER_IP=100        # Requests per minute per IP
# CALENDARBOT_RATE_LIMIT_PER_TOKEN=500     # Requests per minute per bearer token
# CALENDARBOT_RATE_LIMIT_BURST=20          # Max requests in burst window
# CALENDARBOT_RATE_LIMIT_BURST_WINDOW=10   # Burst window in seconds
# Default Timezone (optional, defaults to America/Los_Angeles)
# Used for morning summary and other time-based features
# Must be a valid IANA timezone database name
# Examples:
#   CALENDARBOT_DEFAULT_TIMEZONE=America/Los_Angeles  # PST/PDT (default)
#   CALENDARBOT_DEFAULT_TIMEZONE=America/New_York     # EST/EDT
#   CALENDARBOT_DEFAULT_TIMEZONE=Europe/London        # GMT/BST
#   CALENDARBOT_DEFAULT_TIMEZONE=Asia/Tokyo           # JST
#   CALENDARBOT_DEFAULT_TIMEZONE=UTC                  # UTC
# CALENDARBOT_DEFAULT_TIMEZONE=America/Los_Angeles

# Logging Configuration
# CALENDARBOT_DEBUG=true
# CALENDARBOT_LOG_LEVEL=INFO

# Advanced/Testing (rarely needed)
# CALENDARBOT_NONINTERACTIVE=true
# CALENDARBOT_TEST_TIME=2024-01-01T12:00:00
# CALENDARBOT_PRODUCTION=true

# SSML Conversational Domain (Phase 2 - optional)
# Enable conversational domain for more natural Alexa speech
# Requires specific voices: Joanna or Matthew
# CALENDARBOT_SSML_CONVERSATIONAL_DOMAIN=true
# CALENDARBOT_SSML_CONVERSATIONAL_VOICE=Joanna
